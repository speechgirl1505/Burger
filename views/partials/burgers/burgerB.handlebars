<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-isdevoured="{{devoured}}">
		{{#if devoured}}DEVOUR!{{else}}Poop{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>